# Analytics Plan

Analytics normalizes repositories, issues, pull requests, tasks, and email metadata into dashboard read models. Attention signals are deterministic: overdue tasks, high-priority tasks, open PRs, and unread email. Missing or unavailable sources return an explicit unavailable metric rather than invented values.
