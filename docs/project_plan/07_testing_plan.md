# Testing Plan

Normal tests mock integrations and LLMs. Live scripts remain separate under `tests/api_testing` and must not run during unit tests. Validate API contracts, metric calculations, routes, graph routing, and frontend HTTP helpers.
