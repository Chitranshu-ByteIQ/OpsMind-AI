# Next Steps

1. Integrate the Critic after synthesis with an explicit safe output contract.
2. Add persistent historical storage only if trend analysis is required.
3. Add source-specific health checks and pagination/caching for larger accounts.
4. Start development with `uvicorn app.main:app --reload` in one terminal and `streamlit run app/frontend/streamlit_app.py` in another.
