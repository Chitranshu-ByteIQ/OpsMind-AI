# Streamlit Frontend Plan

The frontend calls FastAPI only. It provides Dashboard, Agent Chat, Needs Attention, and Agent Activity pages, shows connection state without credentials, and handles unavailable backend/source data gracefully.
